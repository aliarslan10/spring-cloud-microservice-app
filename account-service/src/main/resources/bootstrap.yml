spring:
  cloud:
    config:
      name: account-service     # file name for config-server
      profile: native
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
  application:
    name: account-service       # this name appears in discovery-server
